{
  "name": "parallelism-explorer",
  "private": true,
  "type": "module",
  "description": "Static site (no build step). Node is only needed to run the model tests.",
  "scripts": { "test": "node --test 'tests/*.test.mjs'", "serve": "python3 dev.py" }
}
